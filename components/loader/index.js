import Loader from "react-loader-spinner";

const Loader_ = () => {
  return (
    <div className="loader-container">
      <Loader type="BallTriangle" color="#c48445" height={80} width={80} />
    </div>
  );
};

export default Loader_;
